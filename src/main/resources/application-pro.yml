spring:

    datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbd:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8
      data-username: root
      data-password: root
    jpa:
      hibernate:
       ddl-auto: none
      show-sql: true

logging:
     level:
       root: warn
       com.soufuu: info
     file: log/blogdemo-pro.log

server:
  port: 8081

